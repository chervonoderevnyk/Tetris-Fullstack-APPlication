// Production environment configuration
// This file is committed to git and should contain only non-sensitive public config
// For sensitive data, use environment variables or external configuration
export const environment = {
  production: true,
  apiUrl: 'https://your-api-domain.com',
  apiBaseUrl: 'your-api-domain.com'
};